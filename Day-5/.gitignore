# Terraform state files (contain sensitive data)
terraform.tfstate
terraform.tfstate.backup
*.tfstate*
*.tfstate.backup

# Terraform lock file (prevents version conflicts)
.terraform.lock.hcl

# Terraform working directory (stores downloaded providers & modules)
.terraform/
.terraformrc

# Crash logs (if Terraform crashes)
crash.log

# Ignore variable files that may contain secrets
*.tfvars
*.tfvars.json
